node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz.env
.env.local

# 環境變量文件（包含敏感信息，絕對不能提交）
.env
.env.*
!.env.example
*.env

# Replit 配置文件（包含 Semgrep 規則，可能觸發 GitHub 推送保護）
.config/replit/

# 數據庫備份文件（可能包含敏感數據）
backups/
fitbuddy_backup_*.sql
*.backup.sql
# 注意：drizzle/ 目錄下的遷移文件應該保留在 Git 中
